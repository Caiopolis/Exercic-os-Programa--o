const nomes = ["Caio", "Fabio", "Andressa", "Daniel"];
function filtrandoNomes(array) {
  array.forEach((element) => {
    console.log(`Olá ${element}, como vai?`);
  });
}
filtrandoNomes(nomes);


const metodosCalculadora = require("./(parte do exercicio require)")

console.log(metodosCalculadora.soma(3,6))